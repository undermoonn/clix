{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run clix release",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\target\\release\\big-screen-launcher.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "release",
      "console": "integratedTerminal"
    }
  ]
}