[package]
name = "clix_launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.23"
gilrs = "0.9"
walkdir = "2.3"
dirs = "4.0"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser"] }
